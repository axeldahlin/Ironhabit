<div class="container">



{{#if isConnected}}
<h2>Welcome, {{currentUser.email}} </h2>
<h3>You have {{currentUser.totalPoints}} Points!</h3>
{{else}}
<h3>You are not currently logged in</h3>
{{/if}}






<div class="goals-wrapper">
  {{#each goals}}
    {{#if this.currentlyDoing}}
      <div class="goal">
        <div class="goal-title">
          <h1>{{this.title}}</h1>
        </div>
        
        <div class="chart-wrapper">
            {{#each this.displayData}}
              <div class="square color-code-{{this}}"></div>
          {{/each}}
        </div>
        <div>
        

        <br>

        <form action="/update/{{this._id}}" method="POST">
          <button type="submit" class="btn btn-primary">I did this today.</button>
        </form>

        <div>
          <h4>This goal is worth {{this.pointValue}} points.</h4>
        <h3>Current Streak: {{this.streak}}</h3>
        </div>
       
        <form action="/quitHabit/{{this._id}}" method="POST">
          <button type="submit" class="btn btn-sm btn-danger">X</button>
        </form>
        </div>


      </div>
  {{/if}}
  {{/each}}
</div>
</div>